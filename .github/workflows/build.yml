name: Build Android App (Taylor's Version)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4   # 👈 también conviene actualizar esto

      - name: Set up Java
        uses: actions/setup-java@v4   # 👈 opcional pero recomendado
        with:
          distribution: 'zulu'
          java-version: '11'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'

      - name: Install Dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK as Artifact
        uses: actions/upload-artifact@v4   # 👈 🔥 ESTE ES EL FIX
        with:
          name: TheSwiftPlanner-APK
          path: build/app/outputs/flutter-apk/app-release.apk
